# Create a matrix plot

Create a matrix plot of effect sizes and directions per parent for
significant snps.

## Usage

``` r
effectPlot(
  effectDat,
  signSnp,
  map,
  xLab = "Chromosomes",
  yLab = "Parents",
  chrBoundaries,
  title = NULL,
  trait = NULL,
  ...,
  output = TRUE
)
```
