# one position fitted in the mixed model

one position fitted in the mixed model

## Usage

``` r
randomQTLmodel(
  modDat,
  map,
  parents,
  trait = "yield",
  scanMrk = NULL,
  cofMrk = NULL,
  NULLmodel = FALSE
)
```
